var audio;

$("#pausebtn").hide();

function getsong(song) {
    var file = element.attr("file");
    var title = element.text();
    var image = element.attr("image");
    var artist = element.attr("artist");

    song = new Audio("aud/" + file);
}